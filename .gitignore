# Note: .cursor/mcp.json is tracked on purpose so clones get the qlaris server.
# Per-machine MCP overrides belong in the global ~/.cursor/mcp.json, not here.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Logs and temp files
*.log
*.tmp
tmp/

# Research working directories (sensitive research data)
context/
discovery/
